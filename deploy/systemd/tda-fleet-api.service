[Unit]
Description=TDA Fleet API
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/truck-driver-analytics
ExecStart=/opt/truck-driver-analytics/.venv/bin/uvicorn backend.api.main:app --host 0.0.0.0 --port 8080
Restart=always

[Install]
WantedBy=multi-user.target
